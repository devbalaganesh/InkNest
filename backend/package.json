{
  "name": "my-app",
  "type": "module",
   "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@bala_dev/inknest-common": "^1.0.3",
    "@prisma/client": "^6.15.0",
    "@prisma/extension-accelerate": "^2.0.2",
    "hono": "^4.9.6",
    "prisma": "^6.15.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
